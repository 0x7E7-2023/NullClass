* text=auto

# Linux CI 上必须保持 LF
gradlew text eol=lf
*.sh text eol=lf

# Windows 批处理
*.bat text eol=crlf

# 二进制
*.jar binary
*.png binary
*.webp binary
*.keystore binary
# OCR 模型（ONNX protobuf，禁止行尾/编码转换）
*.onnx binary
