# Changelog

本项目的所有显著变更都记录在本文件中。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.9.97] - 2026-09-26

### 新增

- **英文界面**：全部界面提供英文。默认跟随系统语言，也可在「设置 → 界面语言」里手动切换；
  系统语言既不是中文也不是英文时显示中文。

### 变更

- **界面文案重写**：约 260 条中文提示改为面向普通用户的说法，不再出现字段名、文件格式、
  同步内部机制等技术用语；删除确认统一说明「删除后无法恢复」与同步影响；错误提示先说结论和
  处理办法，版本号、错误码等细节放在括号里。
- 课程编辑里的星期选择改为七个等宽按钮。
- **教务导入的选学校页重做**：顶部圆角搜索栏兼作返回；分「学校 / 通用适配 / 我添加的」三页，
  搜索时页签带命中数；上次用过的学校置顶为「最近使用」卡片；学校按拼音首字母分组，
  字母标题吸顶，右侧字母条可点可拖（矮屏收起）；每所学校一张卡片，ⓘ 看详情。
- **适配器清单新增 `initial` 字段**（学校名拼音首字母），学校列表按它分组，多音字地名
  （长春、厦门）不再分错组。官方库全部补齐并发布 v2026.09.23；第三方适配器不填时由应用自动推断。
- **致谢拾光课程表**：README 新增「致谢」一节，应用内「关于」页加上上游链接与说明 ——
  内置教务适配器绝大多数移植自拾光社区的适配库
  [shiguang_warehouse](https://github.com/ShiGuangSchedule/shiguang_warehouse)（MIT），
  「从拾光课程表迁移」也基于拾光公开的导出格式。

### 修复

- 生成二维码失败时显示中文提示，不再直接显示英文原始报错。

## [0.9.96] - 2026-09-22

### 新增

- **适配器库热更新**：导入导出页「从教务系统导入」分组新增当前适配器库版本、上次检查更新
  时间、检查/更新按钮。检查更新会并发问官方源和几个常见公共反代（gh-proxy.com、
  github.dpik.top、gh.927223.xyz、github.tbap.top），取其中最新版本；点击更新后下载并用
  内置公钥验签，通过才会热替换，不用重装 APK 就能拿到教务学校适配的最新收录。
  验签失败或版本不一致一律丢弃，反代不参与信任链。

### 变更

- 「从教务系统导入」分组挪到导入导出页最上面。
- **日程提醒**：改为「提醒」开关 + 下拉选提前量；全天日程只保留开/关（当天 8:00 提醒）。
- 今日页「上课中」卡片去掉描边。
- **适配器库拆为独立仓库** [NullClass-adapters](https://github.com/0x7E7-2023/NullClass-adapters)，本仓库以 git submodule 挂在 `jw-adapters/`，
  构建方式不变（clone 需加 `--recursive`）。「提交适配请求」入口改到适配器仓库。

## [0.9.95] - 2026-09-22

### 新增

- **日程安排**：课表页右上角新增入口（铃铛图标），进入独立的日程页面——月历上有日程的日期
  带圆点，点选日期查看当天日程，下方列出近期日程；新增、编辑、删除都在本页完成。
  - 支持全天或指定开始时间，每条日程可单独设置提醒：不提醒 / 准时 / 提前 5、15、30 分钟 /
    1 小时 / 1 天。全天日程按当天 8:00 计算提醒时刻，与考试一致。
  - 提醒走新的「日程提醒」通知渠道，可在系统设置里单独调整；不依赖学期，没有学期也能用。
  - 日程只保存在本机，暂不参与 WebDAV 同步和导入导出。
- **主题模式**：应用设置里可选「跟随 Material 取色 / 浅色 / 深色」。
- **隐藏「考试」标签页**：应用设置新增开关（默认开启），不用考试功能的可以把底栏收成三个标签。

### 修复

- **课程详情面板关闭生硬**：从面板里点考试的「+」、编辑或删除时，面板先卡一下再突然消失，
  之后再打开也不播入场动画。现在先播完收起动画再执行操作。

## [0.9.94] - 2026-09-21

### 新增

- **平板双栏**：屏幕够宽（约 840dp 以上，平板横屏或大平板竖屏）时，「我的」与「考试」
  改为左右两栏——左边入口列表常驻，右边显示选中的子页，不用来回退返回键。系统返回键
  先收起右栏，收干净了才退出当前页。窄屏一切照旧。
  - 课表页**有意不做双栏**：把 1280dp 切一半后，7 列每列只剩约 65dp，比手机竖屏还挤。
    二维网格需要全宽，课程详情仍走原来的底部面板。
- **手机横屏改用侧边导航**：横屏时底部那条 80dp 的导航栏挪到屏幕左侧，高度全部还给内容；
  配合顶栏滚动时自动收起，课表一屏能看到的节次从约 3 节回到约 6 节——横屏明明更宽，
  过去反而比竖屏少看三分之二。

### 变更

- **课表行高随屏幕高度自适应**：行高原先固定 56dp，平板竖屏下整张网格只占 672dp，
  下方白白空出一大片。现在按可用高度均分（不低于 56dp），平板上 12 节正好铺满，
  手机横屏仍是 56dp 靠滚动看全。
- **正文限宽居中**：设置、关于、导入导出等 16 个页面在宽屏下不再把一行文字拉满整块屏幕，
  正文停在 700dp 居中——否则标题在最左、开关甩到最右，一行要扫过整个屏幕才看得全。
- **横屏的日期选择器改用输入框**：日历式选择器约 568dp 高，横屏（可用高度约 345dp）
  根本放不下，确定按钮会被裁掉。矮屏下直接开输入模式手打日期。
- **教务导入页在横屏下收紧**：说明文字折叠、底部入口跟着列表滚动。这页固定内容累计约
  400dp，横屏下学校列表原本被挤到几乎为零，点开搜索框弹出键盘后一条学校都看不见。

### 修复

- **课程详情面板的「编辑 / 删除」点不到**：面板内容没有滚动，一门课的时间安排多几条、
  或带着考试信息时，底部两个按钮会被挤出屏幕，用户只能取消退出。
- **键盘盖住输入框**：应用开了边到边显示，系统不再为键盘收缩内容区，而全项目没有一处
  处理输入法边距——课程、学期、考试、课表创建、WebDAV 设置这些表单的底部输入框会被
  键盘压住且滚不出来。竖屏同样受影响。
- **横屏时刘海遮挡内容**：主题声明了内容延伸进挖孔区，却没有补偿挖孔边距。横屏时挖孔
  转到侧边，会压住课表最左或最右一整列；「今日」「考试」「我的」同样受影响。
- **转屏丢失教务导入进度**：选中的学校、手填的教务地址、对话框里已输入的内容在旋转后
  全部清空，退回学校列表。此前已经专门防住了「登录到一半误滑一下整个流程就没了」，
  旋转是同一个问题的另一条路径。
- **转屏丢失调课进度**：调课是「先选哪天、再选上哪天的课」两步，第一步选完还没确认时
  转屏，进度会静默归零且没有任何提示。
- **课表格子里的字被裁成半个**：格子放不下时按固定行数截断，省略号不触发、超出部分被
  直接裁掉；系统字体放大后更早发生。现在按格子实际高度决定显示几行。
- **小组件选择器里缺少说明**：添加小组件时只有名字没有一句话说明。

## [0.9.93] - 2026-09-21

### 新增

- **快速删课**（「我的 → 快捷操作 → 快速删课」）：按周次或按某一天筛出课，勾选批量删除。
  一门课在同一筛选下的多条时间安排合成一行（不会让人误以为能只删其中一行），删除粒度与
  课表页长按删除一致——**整门课**，连同它全部时间安排与关联考试；某门课还有安排落在筛选
  之外时，行内会先红字写明「另有 N 条安排在其他日子，删除时一并删掉」。
  - 按日期筛选走与今日页同一套调课解析：这天被调课时列出的是**来源日**的课，并标明
    「这天已调课：上 X 月 X 日的课」；落在学期外的日子如实说明没有课。
  - 勾过但被当前筛选挡掉的课不计入删除——换一周之后「已选 3 门」不会删掉屏幕上看不见的两门。
  - 只想让某一天不上课是另一回事，页头直接把人指去「通知与提醒 → 跳过日期」。

### 变更

- **「快捷操作」改为中枢页**：调课（串课）与快速删课各占一个子页。两件事都自带日期筛选和
  一份列表，堆在同一页里会互相抢滚动位置。

### 修复

- **调课的日期对话框重新显示「第 N 步」提示**：该提示原先作为兄弟节点放在日期选择器上方，
  而对话框容器卡着 568dp 高度上限、内部用 `SpaceBetween` 排布——内容超高后间距变成负值，
  日期选择器反过来盖住了提示，整行一个字都看不见，用户只能靠按钮写「下一步」还是「确定」
  猜自己在第几步。现在提示走日期选择器自己的标题槽。

## [0.9.92] - 2026-09-20

### 新增

- **从拾光课程表迁移**：在拾光「我的 → 高级功能 → 课表导入/导出」导出的课程文件
  （`shiguangschedule_*.json`）可以直接导入，课程、单双周、作息表与开学日期一并带过来。
  「传输 → 从拾光课程表迁移」选文件即可；直接点开那个 json（系统只知道它是 json）走
  通用文件导入时也会自动认出来，不再只甩一句「文件格式不对」。
  - 拾光的**显式周次数组**按《教务适配器移植手册》§4.1 切成「极大段 + 单双周」，
    一行可能切出多个课块；拆成一节一行的连堂会重新合并成一块。
  - 拾光的开学日期按 §4.3 回退到每周起始日那一天再用——直接拿来用会整学期偏。
  - 只设了自定义时间、没有节次的课，落到最接近的一节（差 ≤1 小时）并在导入预览里写明；
    实在对不上才跳过，**不静默丢课**。
  - 学期名取「拾光课表 + 开学日期」（导出文件里没有课表名）：同一学期重复导入按名对齐、
    相当于刷新；春季秋季两份各归各的，不会把上一个学期的课悄悄顶掉。

### 变更

- **新增「我的 → 快捷操作」页，调课（串课）移入其中**：调课原先挂在「通知与提醒」页最底下，
  调休当天要翻到底才找得到，和那页的提醒设置也不是一回事。「快捷操作」收学期当中随时要用一下的
  操作，入口副标题直接显示调课「今天及以后有 N 条」。周视图点星期表头的就地入口不变。

### 修复

- **周视图网格线不再画到课块上**：会话分隔线（上午/下午/晚上）原本是压在课块之上的整行覆盖层，
  跨越分界的课会被拦腰划一道；跨节课块内部的行线也会从课块左右 1.5dp 的留白里各露出一小截。
  现在两种线都在课块下面逐列绘制，遇到课块内部的行边界整段跳过。

## [0.9.91] - 2026-09-20

### 新增

- **调课（串课）**：调休时把某一天设成上另一天的课，如「10 月 11 日周六上 10 月 9 日周五的课」。
  今日页、周视图、桌面小组件与课前提醒四处共用同一套解析，永远取同一天的课；
  上课时间仍按**这一天**的作息——调课换的是「上什么课」，不是作息。
  - 入口一：周视图点某一列的星期表头，本周七天一键选，或「上其它日期的课…」跨周选；
    已调课的列在日期下多一行 `调·周五`，与下面画的课对得上。
  - 入口二：「通知与提醒 → 调课（串课）」两步选日期添加，可看全部、可删。
  - 今日页顶部横幅写明「今天调课 · 上 X 月 X 日（周X）的课」，带一键撤销。
  - 与跳过日期是**两件独立的事**：「周六上周五的课、周五放假」设两条——调休现实就是这两句话，
    合并成「对调」反而表达不了单边的情况。
  - 单双周按**来源日那一周**算：串到单周的周五就上单周课，串到双周的就不上。
  - 只跟一跳、不递归：A 串到 B、B 又串到 C 时，A 拿的是 B **原本**的课，成环不可能。
  - 两端都必须在学期内：学期外本来就没课，串课不能凭空造出课来（日期选择器直接限定范围）。
  - 纯本地存储（新表 `day_overrides`，数据库 v5 → v6），不进 WebDAV 快照与二维码/文件导出——
    调休是「这台设备这个学期」的临时安排，跨设备各自设置比被同步覆盖更符合预期。

### 修复

- **USTC 周日课程错后一周**：适配器按实际排课日期换算空课周次，修正教务周日开周、
  空课周一开周造成的错位，重新计算单双周；缺少学期起始日期时，也按正确的周界推算。

## [0.9.9] - 2026-09-19

### 2026-09-20 重发（versionCode 29）

- **扫码预览兼容性**：优先使用 SurfaceView，修复部分设备的绿色预览，并稳定扫码弹窗的相机生命周期。
- **扫码交互**：增加取景框、识别状态、双指缩放与点按对焦，不显示缩放条和对焦圆圈；自动拉近更平缓，手动缩放后停止自动调整。
- **相册识码**：导入页和扫码页均可选择二维码图片；取消选择返回扫码页，识别校验成功后震动提示。
- **反馈位置**：备份分享、日历导出、文件导入、扫码、WakeUp 和教务导入的错误、结果及处理状态显示在各自分区下。

### 修复

- **扫码导入竖屏被强制横屏**：原先用的 zxing-android-embedded 把扫码页写死成
  `sensorLandscape`，竖着拿手机也会被拧到横屏。改为应用内 CameraX 预览（方向跟
  当前界面走），分析流 1080p + ML Kit 端侧模型；码在画面里太小会按检测框自动拉近。

### 优化

- **二维码负载砍掉四成，明显更好扫**：生成侧不再把 UUID 身份表放进码里（每条 16 字节
  且不可压，占负载四成），码内只留 0/1/2… 索引、扫码导入时重新生成 UUID。典型学期
  （16 门课 × 32 条安排）从 QR version 36（165×165 模块）降到 27（129×129），
  模块大 28%。合并语义与 WakeUp 迁移一致：落到对方当前课表、同名学期按内容对齐，
  重扫新版本课时删除也能传播。旧码（v1 全量 / v2 带表）仍可扫码导入。
- **扫码导入后自动设为当前学期**：此前扫完码界面没反应——学期落在新建的
  「分享者课表」里且不激活，要自己去课表管理切换。现在学期直接并入当前课表，
  合并后立即切换过去（WakeUp 迁移的同款激活路径，且按学期名找——重扫被
  同名对齐换过 ID 也能找对）。

## [0.9.8] - 2026-09-19

### 新增

- **内置适配器 36 → 48 个**（批次四 · 12 所，树维 EAMS 整族）。此前对树维 `/eams/`
  平台零覆盖，这批按脚本实际请求的接口路径普查后整族拿下，三种取数形态齐全——
  走 TaskActivity 接口三连的十件、读已渲染 DOM 表格的（河南科技大学）、
  零请求纯 DOM 的（湖南师范大学）：
  河南理工大学、电子科技大学、郑州航空工业管理学院、郑州汽车工程职业学院、
  大连海事大学、东北大学秦皇岛分校、河南科技大学、合肥师范学院、西安工业大学、
  天津农学院、湖南师范大学、成都信息工程大学。
  周次位图口径在同族里统一（位图下标即周次、第 0 位是占位符，对不上进 `warnings`
  要求核对，不静默产出「第 0 周」）。顺带订正 `masu` / `xzhmu` 的平台归属：
  `/eams/` 是上海树维（新开普子公司），不是强智——只改说明文字，算法不动。
  新件的解析正确性与前几批同档：安全逐条审过、fixture 是合成的（无账号），
  出问题请连适配器名一起报。

### 优化

- **选择周次弹窗自适应大小**：格子和弹窗高度原先写死（固定 5 列 × 320dp），周数少的
  学期弹窗下半截全是空白、超长学期又不够放，横屏/平板宽度也浪费。改为每行格数随弹窗
  宽度自动伸缩（手机竖屏约 5 个，大屏更多），弹窗高度按周数收缩、封顶约半屏，
  超出部分滚动。

### 修复

- **生成二维码对真实课表几乎必然失败**：原先把全部学期（含回收站墓碑）塞进
  Version 40 上限 2953 字节的二维码，gzip 后也超限，按钮等于瘫痪；弹窗里的
  位图还按像素当高度，在 mdpi / 模拟器上等于 1024dp、把对话框撑破。改为只编码
  当前学期的有效记录（省略空字段，UUID 收成索引表，gzip 原文进码不再套 base64），弹窗内二维码按宽度等比缩放。
- **节假日同步长期降级到残缺数据源**：首选源 timor.tech 的旧域名 `api.timor.tech`
  已从 DNS 摘除（NXDOMAIN），所有设备上的同步都静默失败、落到只剩单条法定日的
  Nager.Date——国庆 10.1-10.7 只跳过 10.1 当天，春节/元旦也只有首日。改用官方
  存活地址 `timor.tech/api/holiday/year/{年}/`，并新增第二源 holiday-cn（国务院
  放假安排原文，含假期跨度与调休，经 jsDelivr CDN 国内可直连）。
- **学期跨年时整源被未发布年份拖死**：下一年放假安排要到 11 月前后才发布，之前
  首选源对其返回空数据被当成「源失败」，导致连已发布年份也跟着降级。同步改为
  逐年选源：某年没数据就只影响该年（保留旧缓存或走兜底源），已发布年份始终用
  最好的数据。
- **跳过日期列表跨年条目无年份区分**：跨年学期会把两个年份的条目都列出来，
  日期只显示「M月d日」时「10月1日 · 国庆节」两年各一条、看起来像重复；
  非当前年份的条目现在带上年份前缀。写入前按日期去重，源数据同日多条时
  不再有机会进库。

## [0.9.7] - 2026-09-18

### 新增

- **今日课程小组件支持按钮分页**：课程较多时按小组件高度和字号分配每页容量，
  用「↑ 上页 / ↓ 下页」查看后续课程；每个桌面实例独立保存页数，日期、课程列表
  或容量变化后回到第一页。分页按钮预留独立空间，不再把课程和底部信息挤在一起。

### 变更

- **统一桌面小组件样式**：今日课程采用时间轴卡片，突出正在上课与下一节；
  下节课小组件同步调整排版、配色、深色模式和空状态，小尺寸优先展示课程与时间。
- **保留更新时间**：分页时在页码下方显示「更新于 HH:mm」，单页与紧凑尺寸也保留。
- **关闭系统预测返回动画**：保留正常侧滑返回及原有 220ms 页面淡入淡出。
- 版本升到 **0.9.7（versionCode 26）**：无课表数据库或同步格式变化。

### 修复

- **末页连续点向下导致页码来回跳**：边界按钮显式绑定空操作，回调在状态事务内
  校验来源页码和目标范围，忽略重复或延迟到达的旧页事件。
- **底栏与子页面转场不同步**：子页面进出时底栏与顶层页面处于同一动画图层，
  共同淡入淡出并保持底部占位；Tab 之间切换时使用外层固定底栏，避免闪烁。

## [0.9.6] - 2026-09-18

### 修复

- **周课表网格线从课块里透出**：网格线本就画在课块下层，但课块底色只有 15%~18% 不透明度，
  跨多节的课块会被节与节之间的横线一劈两半，看着像网格线压在了课块上面。现在课块
  （含「非本周」灰块）先垫一层不透明的主题底色再叠半透明课程色，叠加结果与原色一致，
  网格线只出现在空白格与课块边缘的留白里。

## [0.9.5] - 2026-09-15

### 新增

- **「通知与提醒」设置页**：「我的」页里与「导入 / 导出」同级的新入口，把提醒相关的一切
  聚到一处。五项系统权限一站式引导——通知权限、厂商自启动、忽略电池优化、精确闹钟、
  勿扰模式——每项显示当前状态，未授权的一键跳到对应系统页，从系统页返回后状态即时
  刷新。课程 / 考试提醒的提前量选择也从应用设置迁到了这里（应用设置不再重复展示）。
- **精确提醒（可选，默认关）**：开启并授权后，课前提醒改走系统精确闹钟，到点准时；
  未授权或关闭时保持原有 WorkManager 方案（省电策略下有几分钟误差）。授权被授予或
  收回的瞬间自动切换投递路径，两条路径严格互斥，不会双发也不会漏发。
- **节假日在线同步与手动跳过日期**：两个数据源按优先级自动降级——timor.tech（含调休
  补班标记）→ Nager.Date 兜底，全部失败时保留上次结果；按学期覆盖的年份一年一拉、
  一周节流，也可手动立即同步。落在节假日或手动跳过日的**课前提醒不再发送**（考试提醒
  不受影响——考试日期是学校定的）；调休补班日仅在列表里提示，不会凭空生成课程；手动
  添加的跳过日期与节假日撞在同一 天时手动语义优先，不会被同步覆盖。今日视图在跳过日
  显示「今天不上课」提示条。
- **周课表纵横分割线默认开启**：纵向按日期、横向按课节的网格线默认显示（此前默认
  关闭），能见度调至略低于上午 / 下午 / 晚的分隔线；显示设置里的开关不变，此前显式
  关闭过的用户不受影响。

## [0.9.4] - 2026-09-15

### 新增

- **教务导入后自动切到导入的那份课表**：以前导入新学期之后，界面上还停在上一个学期
  （导入的学期 `isCurrent` 一律是 false，只有「一张课表里一个当前学期都没有」时才会自动
  激活），用户得自己去学期管理里点一次。现在教务导入的语义就是「开始用这份新课表」：
  合并时把载荷里**开学日最新**的那个学期标成当前学期，同课表里原来的当前学期让位；
  「一键刷新」同一学期时标记留在原地，但教务纠正过的开学日 / 总周数会真正写进本地。
  `.nullclass` 备份、扫码、WakeUp 迁移的行为不变。

### 修复

- **导入教务课表后周视图显示的不是本周的课**：从教务系统导入之后切到「课表」页，
  部分课程没有渲染出来（今日页正常），重启应用才恢复。根因是周视图翻到的周次记的是
  一个裸数字——导入把当前学期整个换掉之后，上一个学期翻到的「第 5 周」被带进新学期，
  而新学期此时可能才第 1 周；那一周不上的课块（下半学期开课、单双周）于是整片消失，
  看着像课没画出来。现在翻到的周次**连同当时的学期身份与开学日一起记**，换学期
  （或教务纠正了开学日）之后这个选择即作废、回到跟随本周；显示的周次同时会夹进
  学期范围，学期总周数被改短时不会停在一个不存在的周次上。

## [0.9.3] - 2026-09-13

### 新增

- **内置适配器 23 → 36 个**（批次三 · 13 所，一个平台一族）。这批按「与已移植上游脚本的
  相似度」选型，覆盖强智 `jsxsd`、正方新版 `jwglxt`、金智 `jwapp`、以及一个**零接口请求**
  的纯 DOM 件：
  长春工程学院、湖南工程学院、南昌航空大学科技学院、湖南商务职业技术学院、
  徐州医科大学（研究生）、塔里木大学、西安文理学院、浙江中医药大学、甘肃医学院、
  青岛黄海学院、重庆化工职业学院、武汉理工大学、东北大学。
- 其中**长春工程学院走 WebVPN**（课表页主机是网关现场编码的子域，与登录域不同源）、
  **南昌航空大学科技学院用非标准端口**、**武汉理工大学/东北大学**是金智族里模块与字段
  都不同的两个部署。

### 修复

- **一批「静默算错周次」的缺陷**（各自独立，形状不同，均已补用例 + 变异测试）：
  - **「单/双」写在逗号后自成一段时被丢掉**（塔里木大学、浙江中医药大学、湖南商务
    职业技术学院）：`1-16周,双` 会静默塌成「每周都上」——这正是测试方案里点名过的
    事故形态，标记必须绑到最近的周次段，两头都没有时出声。
  - **区间分隔符只认半角连字符**（长春工程学院、南昌航空大学科技学院、湖南工程学院、
    塔里木大学、浙江中医药大学、青岛黄海学院、重庆化工职业学院）：`1～16周`
    （全角波浪）、`1－16周`、`1−16周`、`1–16周`、`1 -16周`（破折号两侧带空格）
    会静默变成**只上第 1 周**，整学期的课少上十几周。现在分隔符先归一，且
    **破折号两侧的空白一并吃掉**。
  - **同一格写两段周次时第二段整段消失**（长春工程学院、南昌航空大学科技学院）：
    改成逐段产出，两段各自成块。
  - **括号里的教学班序号被当成周次**（东北大学、徐州医科大学）：`(1)1-16周` 会读成
    「11-16 周」（少掉 1-10 周）、`1-16周(1)` 会把总周数抬到 30。现在括号里的纯数字/
    区间整组先删，且带「节」的分段不再参与周次。
  - **节次越界会产出非法时刻**（湖南商务职业技术学院、长春工程学院）：越界节次此前
    会被照收或补出 `24:00`/`85:45`，那会让**整份载荷被拒**，不是丢一门课。现在一律
    截断或跳过并出声。

- **修掉一批「用例看着在测、其实没测」的覆盖缺口**（变异测试实测「改坏后仍全绿」后补
  的用例）：湖南商务职业技术学院的「按网格列号对星期」整支、长春工程学院的节次上限与
  占位作息、武汉理工大学的「总周数取校历」、甘肃医学院与青岛黄海学院的「开学日回退到
  周一」。

- **`totalWeeks` 上限在两层不一致**：载荷层允许到 30 周、而领域模型与学期编辑界面只允许
  25 周 —— 从适配器导入一个 26-30 周的学期时，导入能过、**写进库再读出来会崩**。
  现在四处（域模型、两个编辑界面、载荷校验）共用一个常量 `MAX_TOTAL_WEEKS = 30`；
  顺带修掉 WakeUp 导入路径把 26-30 周静默截到 25 的行为。

- **东北大学适配器不再请求「当前登录用户」接口**（金智 `currentUser.do`）：该接口按设计
  返回登录人档案，落在内置适配器「不请求个人信息类接口」的红线上；它只做学期编号的降级
  兜底，后面本来还有按日期推算，删掉零代价。青岛黄海学院的 `AUDIT.md` 也改准了请求面
  声明（三条请求里有一条是移植时新增的，此前写成「都来自上游」）。

## [0.9.2] - 2026-09-12

### 修复

- **高版本安卓返回手势偶发「系统预测动画打架」**（targetSdk 36 起预测返回默认开启，
  Android 14+ 真机）：返回手势提交的瞬间返回栈就变浅，但页面退出转场还在播（默认
  700ms 淡出）——这个尾部窗口里快速连滑的第二次返回落到系统手里，插播「退到桌面」的
  系统预测动画、和还在播的应用转场叠在一起，松手甚至会直接退出应用。现在转场缩到
  220ms，且退出转场未落定期间由兜底 BackHandler 吞掉返回；预测返回手势拖拽时的页面
  预览动画（spring 淡入 + 缩小）不受影响。
- **返回后快速点击偶发直跳浏览器**（转场「僵尸点击」）：正在退出动画中的旧页面在
  转场结束前仍可点击、且压在新页面之上——从「关于」返回「我的」后快速点
  「导入/导出」，点到的其实是还没退干净的「关于」页同位置的「源码仓库/反馈问题」
  链接行。现在所有导航页退出转场期间自动屏蔽触摸，点击直接落到新页面；转场中误点
  旧页任意按钮（双重导航、误触返回）这类同源问题一并修掉。
- **教务导入页系统返回手势直接退掉整个导入流程**：选校进入 WebView 步骤后滑返回，
  整个导入 Activity 被退掉，与顶栏返回键「回学校列表」的行为不一致，登录到一半误滑
  全丢。现在手势与顶栏行为一致。

### 变更

- 版本升到 **0.9.2（versionCode 21）**：无数据/同步格式变化。

## [0.9.1] - 2026-09-12

### 新增

- **内置适配器再添 12 所学校**：11 → **23 个**。选型是数据驱动的 —— 按「与已移植上游脚本的
  token 相似度」给剩余 205 个排序，挑出 12 个 **≥0.72 的近克隆**（正方 4 / 强智 6 / Struts2 1 /
  for-std 1）：重庆财经学院、淮北理工学院、桂林医科大学、新疆工程学院、文华学院、
  湖南信息职业技术学院、中国石油大学(华东)、河南财经政法大学、怀化学院、重庆人文科技学院、
  广西电力职业技术学院、郑州大学。方法见新增的 `docs/jw-adapter-porting.md`（移植手册）与
  `docs/jw-adapter-testing.md`（测试方案）。
  - 「同平台可复用」是**分层**的：接口信封与行字段名常能复用，**取数路径与周次/节次编码未必**
    —— 有两位移植者去翻同平台**别的**上游脚本取证，才发现自己那份漏掉的编码写法。

### 修复

- **批次二审查确认的 13 条缺陷**（两名 reviewer + 独立证伪者用真实浏览器与 Node 实跑复现，
  全部修掉并补了回归用例）。五条会让**整次导入失败**或**静默丢课**：
  - **节次越界造出非法时刻**（`重庆人文科技学院`、`中国石油大学(华东)`）：作息表顺延时没夹 24:00
    上界，出现 `24:10`、`85:45` 这种时间，**整份载荷过不了校验、导入直接失败**。而 `[12345678节]`
    这类脏数据会一路补到第 78 节。
  - **警告文本超长导致载荷被拒**（`重庆人文科技学院`）：「已跳过」那句把课名与教师拼进正文且不截断，
    5 个长课名就到 201 字，**超过 200 字上限 → 整次导入失败**。
  - **读不出「周在括号里」的写法**（`重庆人文科技学院`）：`1-16(周)` 是上游自己的**主要形态**
    （它源码注释里就写着），却被判成认不出 → 全部块被丢、单门课时直接报错。
  - **表头没认全就静默丢课**（`广西电力职业技术学院`）：只认出周一到周五时，周末的课整门消失
    且一声不吭。
  - **星期按数组下标对齐**（`重庆人文科技学院`、`广西电力职业技术学院`）：行中部有合并格时整行错一天。
  - 其余：`(1-2)` 教学班序号被当成周次（`淮北理工学院`/`怀化学院`/`广西电力职业技术学院`）、
    空白隔开的「单/双」被丢掉（`广西电力职业技术学院`）、写在周次**前面**的「单周/双周」读不到
    （`重庆人文科技学院`）、`SPEC_TITLE` 含「时间」把钟点区间当周次凭空造出排课
    （`中国石油大学(华东)`/`广西电力职业技术学院`）、整对括号连内容一起删导致课被丢（`文华学院`）。
- **`衡阳师范学院` 无星期表头时整行错位一天**（批次一遗留）：兜底用「本行格子数」推「最后 7 列」，
  行中部有合并格时按错列算星期，而那条路径**从没被测过**（原有的唯一用例带完整表头，走不到它）。
  现在 `extract.js` 交出网格列号（含 rowspan 跟踪）、`parse.js` 按网格列对齐，并补了专门用例。
- **三处「声明比代码宽/窄」**：`河南财经政法大学` 声称「停在任意一页都能提取」（实际只有教务主机能，
  空 `allowHosts` 下别的主机会被沙箱拦）、`新疆工程学院` 的读取面表漏了「读主机名」这一条
  （它会进错误日志）、`重庆人文科技学院` 的 `allowHosts` 从整校域收窄为
  `*.webvpn.cqrk.edu.cn` + 两个精确主机（原 AUDIT 断言「没有更窄又能工作的写法」是**假二选一**，
  同批的 `中国石油大学(华东)` 用的正是并集写法）。

### 变更

- 版本升到 **0.9.1（versionCode 20）**：同步格式仍是 formatVersion 2，与 0.9.0 完全互通。
  **内置适配器不随本版新增信任等级**：这 12 个与批次一那 8 个同档 —— 安全逐条审过，
  但解析正确性没验（无账号），fixture 是合成的，出问题请连适配器名一起报。
- 适配器用例从 23 个增加到 **86 个**；`SPEC_TITLE` 的口径在 6 个强智/正方适配器间统一
  （不许把「时间」并进来），并在各自的 fixture 里塞了一个**该被忽略的诱饵 font** 当守卫 ——
  将来谁把匹配写宽，用例立刻变红。

## [0.9.0] - 2026-09-12

### 新增

- **适配器可以问用户了（脚本弹窗）**：教务适配器脚本新增三个提问接口 —— `__ncSelect`（选项）、
  `__ncConfirm`（是/否）、`__ncPrompt`（文本输入），都返回 Promise。有些教务系统不问一句就拿不到
  课表（选学年学期、补一个页面上没有的值），此前适配器只能猜、或者干脆做不了。
  **取消是正常结果**（`select` / `prompt` 得 `null`，`confirm` 的「否」得 `false`），只有参数不合法、
  超过次数上限、桥不可用才会 reject —— 适配器据此安全退出。`__ncSelect` **只回传下标**，
  不回传文本（脚本拿不到用户没选的东西）。单次提取最多问 8 次。
  - **弹窗抬头由应用渲染，适配器改不掉**：固定写「适配器「X」在向你提问」，`__ncPrompt` 底部固定附
    一句「空课不会向你索要教务密码等账号信息」。标题、说明、选项文本都来自第三方脚本 ——
    不这样标一句，恶意脚本就能在应用自己的界面里伪装成空课去钓账号。
  - **等用户回答的时间不算脚本超时**（超时是为了兜住跑飞的脚本，不是催用户做决定），
    但等待自己封顶 5 分钟，界面异常时不至于永远挂着。
  - 参数全部在纯 JVM 层校验与限额（标题 ≤60 字、说明 ≤300 字、选项 1–50 项、输入 ≤200 字、
    `maxLength` 1–200），越界的默认项只夹取不判死：弹窗照样弹，用户自己选。

- **适配器可以要求「重点核对」**：载荷新增 `warnings`（≤20 条、每条 ≤200 字），适配器在这里
  如实说明哪些字段是推算出来的，导入预览会**单独成段并标明来源**（「以下说明由适配器
  （第三方脚本）提供，不是空课官方的判断」）—— 用户必须能分清哪句是空课说的。要它是因为
  **开学日期决定「现在第几周」**，而一多半上游教务给不出这个日期，只能推算，推算值在库里
  跟别的字段长得一模一样。顺带兑现了一个一直没实现的承诺：`ocrAssisted: true` 此前是个
  纯死字段（规范写了「应用会在导入预览里加核对提示」，实际没有任何界面读它），现在真的会
  变成预览里那条固定提示。

- **内置适配器新增 8 所学校**：从上游社区适配仓库（拾光课程表，MIT）移植，一个教务平台一个 ——
  浙江工业大学（正方新）、衡阳师范学院（强智）、南京工业职业技术大学（金智 jwapp）、
  江苏旅游职业学院（树维 for-std）、马鞍山学院（树维 EAMS）、东北农业大学（URP，走 WebVPN）、
  山西工程职业学院（超星托管）、湖北医药学院（Struts2）。内置适配器从 3 个变成 11 个。
  做法与判据见新增的 `docs/jw-adapter-porting.md`（移植手册）与 `docs/jw-adapter-testing.md`（测试方案）。
  - **审计的是安全性，不是正确性**：这 8 个的代码逐个人工过了「不碰凭据、不外发、只读课表、
    不埋点、不写页面」八条，请求域逐个核对并写进 `allowHosts`；但**解析对不对没法验**
    （我们一个账号都没有），所以 fixture 是合成的 —— 只保证「同样输入同样输出」。
    这 8 个学校的正确性靠用户反馈迭代，出问题请连适配器名一起报。
  - 移植过程中修掉的坑（都在上游脚本里就存在）：`1-16周(双)` 这类「单/双写在周字后面」的写法
    会被解析成**每周都上**且一声不吭；括号里的序号 `(1)` 会被当成周次，把教师、教室、真实周次
    一起吃掉；某行读不出「第N节」标签时整行的课**静默消失**；接口没给记录总数时只取第一页就停，
    而「可能不完整」的提示也被抑制。这些都补了独立推出的回归用例。

### 变更

- **能力位 `__ncCapabilities` 新增 `ask`**：要求「宿主开了 **且** 桥对象已注入 **且** 三个全局真的
  定义了」三者同时成立。只报开关会撒谎（0.5.0 的 `ocr` 就是这么坑过适配器的），适配器会拿着
  `undefined` 去调用然后静默失败。
- **适配器规范 §5 拆成 §5.1 OCR / §5.2 提问**，并写明**提问只应出现在 `extract.js`** ——
  `parse.js` 在 CI 里用 Rhino 跑，那里没有桥也没有用户，它必须是纯函数。
- 脚本桥拆成三块：传输与路由、OCR、提问各管各的（两个通道各有各的 pending 表，串了就是永不 settle）。
- 版本升到 **0.9.0（versionCode 19）**：同步格式仍是 formatVersion 2，旧版备份 / WebDAV 快照
  与新版互通。新增的 `warnings`（见「新增」）是**可加字段**，所以这批移植适配器的
  `minAppVersionCode` 一律写 11 —— 老版本忽略它、导入照常成功，只是看不到那几句核对提示；
  这一项卡的是「老版本整个不认」的结构性能力（例如 `kind:"boxes"` 这类载荷种类），不卡客套提醒。

## [0.8.2] - 2026-09-12

### 新增

- **课表管理：一张应用装多张课表**：课表从「应用本身」解耦成一等对象。**新装应用打开第一件事
  就是创建课表**——顺手建第一个学期（默认下周一开学、20 周、默认节次表），建完即有可用课表；
  「我的」里学期管理的同级新增**课表管理**（切换 / 新建 / 重命名 / 删除）。不同课表的数据
  **结构性独立**：学期、课程、节次时间全挂在各自课表下（不是靠过滤维持），切换课表后
  今日页 / 周视图 / 小组件 / 提醒整棵跟着切，互不串。当前课表是**本地**选择（不同步）：
  两台设备各看各的课表；删当前课表自动落到剩下最早的一张，**最后一张不许删**（删光就没有
  「当前」可言）。周视图顶栏只在课表多于一一时前缀课表名——单课表用户界面一个字不变。
  导入导出：教务 / WakeUp 落**当前课表**；`.nullclass` / 扫码按文件里的课表还原——
  **导入同学的备份会多出一张课表**（导入完成的提示会指明），而不是把他的课混进我的课表；
  旧版本的文件没有课表字段，照常导入、落当前课表（formatVersion 不动，老版本读写都不受影响）。

- **节次时间快速设定**：学期编辑页在节次时间表上方加了两个输入框 —— 单节课分钟数 + 课间休息分钟数，
  点「套用」按这两个数重排整张作息表（直接填数字，不用一格一格点）。
  **课间休息指一个大节里两节之间的休息**（默认模板是 10 分钟）。**大节与大节之间的休息不归它管**：
  那种（上午大课间）各校差得多，它就等于相邻两个大节开课时刻之差，套用时锚点不动、原样保留。
  所以对着默认模板套「45 + 10」是恒等变换，两格初值也从现有作息里推出来（一打开编辑页就跟现状对上）。
  排不下时（大节间距比「单节 + 课间 + 单节」还短）就地提示是哪个大节顶到了下一个，不会落下半截作息表。
- **学期管理可编辑 + 每周起始日**：学期管理列表每行加了编辑入口（此前只有当前学期能从「我的」
  顶部卡片改），学期周数、第 1 周日期、节次时间表都在那里改。新增**每周起始日**
  （只给周一 / 周日两个选项 —— 教学周几乎只有这两种排法；要更细就直接改第 1 周的日期，
  一周从哪天算起本来就是那个日期的星期几）：一周从哪天算起，第 1 周就从哪天开始，
  周视图的列顺序、翻周边界与「第几周」都跟着走 —— 一周从周日算起的学校不再整体错位一列。
  起始日与日期是同一个值的两种说法（不另存字段，免得出现「日期是周一、却说一周从周日算起」
  这种自相矛盾的学期）：点起始日会把日期挪到**最近的**对应星期几（±3 天内，周一 ↔ 周日
  来回切也回到原处，不会每切一次漂一周），编辑页当场预览第 1 周的起止日期。
- **显示非本周课程（可选，默认关闭）**：设置页「课表显示」与课表右上角「显示设置」各有一个开关
  （同一个偏好，两边同步）。开启后，本周空着的时段会补上**别的周要上的课**——单双周的另一半、
  上半学期的课——一律主题灰、低透明度，不跟课程色走（没有颜色可认，就不会和任何一门真课混淆），
  块上分三行写清楚**「非本周」**、课名、它自己的周次（`1-16周·单`）：
  光有一块灰，用户看不出它是什么意思。点灰块同样能打开课程详情、高亮对应的那条安排。
  只补**当周空着**的时段：和真课块撞上的整块不画（裁短了会谎报它只有那么长），
  同一时段在别的周有好几门课时按开始节次先到先得（画两个只会糊成一团）。
- **适配器搜索**：教务导入页的适配器列表顶部加了搜索框。匹配范围是学校名、key、教务域名与作者
  （`dlutci`、`jw.dlutci.edu.cn`、`大连 工程`、`城市学院` 都找得到）；大小写不敏感，
  多个词按「全部命中」算，查询里的空白与顿逗分号斜杠都当分隔符（全角空格、从网页复制常带上的
  不换行空格也算）。**从适配器库添加时的候选列表共用同一个搜索框**——
  社区库几十上百个适配器时不必再一行行翻。搜不到任何东西时直接把置底的「通用适配器」摆出来
  （前面一句说明）：「搜不到我的学校」正是它存在的理由。

### 修复

- **开学前一周被算成「第 1 周」**：`Term.weekOf` 用整数除法定周次，而 Kotlin 的整数除法向零
  取整 —— 开学日之前 1~6 天全被判成第 1 周。9 月 7 日开学时，9 月 6 日那个周日因此被当成
  「第 1 周」：今日页会列出还没开学那周的课，提醒也会照排。改用向下取整。
- **课表页把「今天不在学期内」说成「第 1 周 · 本周」**：上面那条修正让 `weekOf` 在开学前
  几天与学期结束后返回 null（今日页、提醒、小组件、学期列表、我的 都据此显示「不在学期内」），
  课表页的 `?: 1` 兜底没跟着改：顶栏照旧写「第 1 周 · 本周」，第 1 周那一列还按今天加粗高亮、
  画上当前时间线 —— 那一列的日期与今天差 1~6 天（学期结束后差几个月）。现在「本周」标记、
  今天列高亮、时间线、「回本周」按钮一律以「今天真的在这一周」为准。
- **快速设定排到第二天会闪退**：`retimeSections` 只比「下一个大节的开课时刻」，而最后一个大节
  后面没有可比的对象 —— 晚上开课的作息（删过行、教务导入的都长这样）配上大一点的单节时长，
  就会算出 `endMinuteOfDay > 1440`，撞在 `PeriodTime` 的 init 上抛异常，点「套用」当场闪退。
  现在越界返回「已经排到第二天」，就地说明是哪一大节，既不崩也不改坏作息表。
- **`24:00` 存不回去**：模型允许 `endMinuteOfDay = 1440`（当天最后一刻），编辑页也显示得出
  `24:00`，但保存那条解析路径只收 0..23 点 —— 快速设定刚写好的行保存时弹「时间格式应为 HH:mm」，
  用户对着输入框里明明白白的 24:00 无从下手。解析多认这一个值，让文本与分钟数能往返。

### 变更

- **课表管理、学期编辑、节次快速设定、非本周灰块、适配器搜索**随本版发布。
- **Room schema v3**：`timetables` 表 + `terms.timetableId`；v2 存量学期升级后归入固定 id 的
  「我的课表」（两台设备各自升级也收敛为同一条，不会同步出两张空的）。迁移经真机覆盖安装验证。
  **移除 `fallbackToDestructiveMigration`**：v2 已经发布上线，留着它意味着哪天漏写一个迁移、
  升级用户的课表会被**静默清空**——宁可当场崩出来，也不能闷声删库。
- 版本升到 **0.8.2（versionCode 18）**：同步格式仍是 formatVersion 2，旧版备份/WebDAV 快照
  与新版互通（新字段都带默认值，旧版本读写不受影响）。

## [0.8.1] - 2026-09-12

> 通用适配器（下面「新增」里的那批内容）没有单独发布过 0.8.0，随本次一起首发。
> 上线前在真实教务系统上做了一轮实机验证，暴露出的问题都记在「修复」里。

### 新增

- **通用适配器（置底兜底）**：没有专门适配器的学校，填一次教务地址就能导入。它不认学校——
  把页面读成「文字 + 坐标」清单，交给**与 OCR 课表完全共用**的表格结构层还原（锚定星期表头行
  → 7 列网格 → 节次列定行）；课表是图片/画布画出来的（青果那类）则退回离线 OCR。
  地址会被记成「一键刷新」的入口，下次直接进；在适配器列表里单独一栏排在最下方，
  不与具体学校混排。识别结果仍然要用户核对后才入库。
- **适配器规范新增 `boxes` 载荷**：任何适配器都可以只交出「文字 + 坐标」，由应用还原行列
  （规范 §4.2）。定位是「页面结构完全不可预期」时不用在脚本里认表格。
- **适配器规范新增清单字段** `startUrlPrompt`（地址由用户现场输入）与 `fallback`（列表置底）。
- **节次列多行标注也能定行**：一格里「节次 + 上课时间」叠在一起（`3-4\n10:00-10:45`）时，
  逐行找合法节次标注。此前认不出就整张表找不到行锚点。
- **总周数按表内最长周次自动抬高**：出现 `1-22周` 时不再因为「超出默认 20 周」被判成认不出周次、
  静默退回整学期。

### 变更

- **内置示例适配器（`example-univ`）删除**：它原本的职责是「演示全链路」，
  现在由通用适配器（真能用）与两个真实学校适配器（`ustc` / `dlutci`，真能照着写）取代。
  索引、README、适配器规范里的示例引用一并换成真实适配器。
- **「一键刷新」记住的地址改为提取时实际所在的那一页**（此前记的是本次入口地址）：
  重进时直接落到课表页，而不是从登录页再走一遍。
- 版本升到 **0.8.1（versionCode 17）**：通用适配器要求 `minAppVersionCode: 16`
  （旧版本不认识 `boxes` 载荷）。
- **通用适配器不再「自动提取」**：它的课表是页面自己渲染出来的，而页面加载完（`onPageFinished`）
  不等于课表画好了。提取一开始就冻结网络，抢跑会把还在加载资源的页面（资源常放在 CDN 域上，
  冻结时一并被拦）掐死在白屏上——金智课表真机实测如此。它的文案本来就写着「打开课表页后点提取课表」。

### 修复

真机上第一次拿真实教务系统（金智 jwapp 学生课表）跑通用适配器，暴露并修掉：

- **提取直接失败：这一页里找不出课表结构**。课表格子跨多节时（连堂）文字靠在格子顶端，
  行归属原本判「离行锚点不超过半个行距」，于是课名整片落空（真机 113 个文本框里 44 个被判成
  「落不进网格」，占 39%，超过 20% 的可靠性红线）。改为按相邻行锚点的**中线**切段定行。
- **叠在同一格里的多门课会糊成一门**：周次错开的课在教务页面上就是并排画在同一个格子里的
  （星期三第一二节可以叠着两门课）。结构还原改为**按列分组**（列靠得住——格子里的文字在列内居中），
  再按「一段以周次行收尾」切段，一门课一段。
- **一段文本里的多段周次只取第一段**：`2-5周,9-17周,星期1,1-2节,教学楼105` 此前只导入 `2-5周`，
  `9-17周` 静默消失（学生少上半个学期的课而不自知）。现在整行所有周次段都会成块，
  单双周标记也按每段各认（`2-6周(双),10-16周(双)`）。顺带支持 `1,3,5-8周` 这种散列写法。
- **连堂被记成单节**：格子里写明的节次（`1-2节`）此前不作数，只按行锚点记起始那一节。
- **教室整条丢失**：教室跟在周次同一行（`…,1-2节,教学楼105`）时，逐行找「整行是教室」的写法认不出。
- **课名带上课号与教学班号**：`2010020 C语言程序设计[05]` 会原样进课表，同一门课的不同教学班
  （`[05]` 与 `[05S01]`）还会变成两门课。现在剥掉课号前缀与教学班后缀。
- **跨 4 节的课多出一倍课块**：这类课被页面在相邻两个格子里各画一遍，现在按块去重。
- 教室识别不再误伤课名：教学班号里的 `S01` 会被「字母 + 2-4 位数字」的教室规则命中，
  导致 `6120011 职业生涯与发展规划[03S01]` 这类课名行被当成教室、课名落到教师名上。
- **通用适配器的健壮性**（同一次真机验证里顺手加固的，都是别的教务系统会踩的形状）：
  - 星期优先信格子里写的「星期N」：一格横跨几个星期列、或表头排偏一列时，列会指错日子。
  - 节次列的判据从「页宽的左侧 1/4」改成「在星期列外侧」：同一张表在容器坐标与整页坐标下页宽
    差好几倍，按比例判会在其中一种坐标里整列落空；右侧排节次列的系统现在也认。
  - 行标是上课时间（`08:00-08:45`）而不是节次号时，拿它定行，节次号按行序推断 ——
    推断出来的东西会写进「需要重点核对」，不会悄悄用。
  - 整页格子里没有周次信息时（「本周课表」那类视图）退回逐格还原，而不是把一整列拼成一门课。
  - 结构层要求核对的话（表头顺序异常、节次号是推断的……）现在会真的出现在核对对话框里，
    以前只留在内部 warnings 里，用户看不到。
  - 兜底路径（周次认不出）也记教师了：此前这种格子的教师会丢。
- **通用适配器弹窗里填的地址被「上次成功那页」顶掉**：适配器是上次用过的那个时，
  `preferredUrl` 优先取记住的课表页，用户刚在弹窗里改的地址被丢掉 —— 换学校、修失效地址
  仍然无门（真机实测：填了新地址，进去的还是旧地址）。现在弹窗里填的地址优先。
- **可靠性判据把表格之外的页头页脚算了进去**：整屏截图必然带上页头导航与页脚版权，
  它们按「落不进网格」计入比例，能把一张干净课表压到 33%、直接判不可靠拒识（真机 OCR 实测）。
  现在只有落在课表纵向范围内的文本框才算候选。

## [0.7.4] - 2026-09-12

### 新增

- **迟发补发（已开始提醒）**：进程被杀或省电策略压住导致课前提醒没赶上时，
  下次重排（打开 App / 数据变化 / 每日维护）会对「已开课未下课且开课不超过 30 分钟」
  的课补发一条「已开始」通知；已发过的落键去重，不会复活用户已划掉的通知。
  未授权通知权限时静默（不落键，授权后仍可补上）。
  同时修复既有缺口：重排先 cancel 全部提醒任务，提前量窗口内（提醒时刻已过但课未开始）
  的课既不补发也不重排——现在这类课重新入队立即发，已发过的跳过不复活。
- **后台可靠性引导（零新权限）**：设置页新增「后台可靠性」节——电池优化豁免状态
  （红/绿灯，从系统页返回自动刷新）+ 跳系统电池优化列表 + 识别到厂商 ROM（小米/
  华为/荣耀/OPPO/一加/vivo/iQOO/魅族）时提供「自启动」设置跳转（打不开回落应用详情页）。
- **今日小组件「更新于 HH:mm」**：中/大尺寸在标题行右侧显示数据计算时刻，
  跨天兜底失灵时用户能一眼看出课表是不是旧的。
- **小组件系统级 30 分钟兜底刷新**：`updatePeriodMillis` 从 0（关死）改为 30 分钟——
  进程死后跨天陈旧上限从「下一次每日维护（最长近 24h）」降到 30 分钟；App 活着时
  推送轨道照常更勤，这只是最后防线。
- **每日维护对齐 00:05**：跨天刷新不再取决于用户哪天装的 App（旧版从入队时刻起算
  随机相位，早上的小组件可能一直是昨天的课）。旧名任务一次性取消迁移。
- **系统事件对账（开机/更新/改时间/换时区）**：`SystemEventReceiver` 在
  BOOT_COMPLETED / MY_PACKAGE_REPLACED / TIME_SET / TIMEZONE_CHANGED 后立即刷小组件
  + 重排提醒（含迟发补发）。WorkManager 的 delay 不感知墙钟跳变，改时间后此前无人纠正。
  BOOT_COMPLETED 需声明 `RECEIVE_BOOT_COMPLETED`（normal 权限，安装即授予，无用户操作）。

### 修复

- **小组件刷新轨道异常自愈**：时钟轨/数据轨/字号轨一次 `updateAll`（Glance IPC）
  或 Room 读异常会静默杀死协程，分钟级刷新从此停摆且无任何日志；现在跳过当拍、
  记 logcat、退避后继续（`WidgetAutoUpdater`）。
- **每日维护失败会重试**：`DailyMaintenanceWorker` 此前无条件 `success()`，一次失败
  小组件可陈旧到用户手动打开 App；现在 `retry()`。同时去掉 `battery-not-low` 约束
  （每天一次的兜底不该在低电量时静默缺席），升级后用 `UPDATE` 策略刷新旧任务。
- **通知投递异常不再吞掉**：`notify()` 抛异常时记日志且不算「已发送」（`ReminderNotifier`）。
- **重排空窗消除**：提醒重排从「先 cancelAllWorkByTag 再全量入队」改为「先入队、再按
  名单取消不再需要的」——取消与入队之间有多个 suspend 点，中途进程被杀会留下
  全部提醒丢失的空窗。
- **提醒去重键容差匹配**：已发送键是墙钟 epoch（blockId:startAt），时区切换后同一节课
  重算的 epoch 会漂移、精确匹配失效，换时区会立刻重复通知（或重复补发「已开始」）；
  现按同 blockId ±24h 容差匹配（同块课每周最多一次，不会误伤相邻周），时钟回拨后的
  重入队重发也一并堵住。

## [0.7.3] - 2026-09-11

### 新增

- **学期管理**：我的 → 学期管理。列表切换当前学期、删除学期、新建学期；顶部学期卡片仍进入当前学期编辑。删除当前学期后激活开学日最晚的剩余学期。

### 变更

- **今日课程小组件最小宽度改为 3 格**：2 格宽时时间列占满、课名几乎看不见。窄位用「下节课」。
  已钉在桌面上的 2×2 实例不兼容，需重新添加。
- **今日课程小组件按配额展示**：已上完的课不占行；同课连堂（节次相邻）合并为一行；
  按实际高度、小组件字号档、系统 fontScale 和 RemoteViews 中文行高计能放下几门
  （按 1sp=1dp 会少算约四成）。课行配额留出的空白里靠左另起一行「还剩 n 节」，不为脚注少排一门。

## [0.7.2] - 2026-09-10

### 新增

- **桌面小组件字号**：设置里小 / 标准 / 大 / 特大四档（0.9 / 1.0 / 1.25 / 1.5），只作用于 Glance；
  时间列宽随字号一起放大。字号在 Glance composition 里 collect（`updateAll` 不会重跑
  已活着的 `provideGlance`，否则只能改动第一档）。特大档长课名可能被裁切（Glance 无省略号）。
- **关于页**：我的 → 关于。版本号（点按复制，方便填 bug 报告）、源码仓库、GitHub Issues、GPL-3.0；
  页脚列出 GitHub 贡献者。
- **教务导入按课名上色**：归一化时用关键词库落到 12 色板（思政红、体育绿、外语蓝、数学橙等）；
  未命中的课仍按顺序轮转。不加领域字段；手动改过的颜色再导入仍保留。
- **清空本学期课程**：学期设置底部，二次确认后软删除当前学期全部课程与课块，学期和节次表保留；
  墓碑随 WebDAV 同步，小组件/提醒随课表 Flow 更新。

### 修复

- **课表与底栏之间的横条空白**：内层 Scaffold 把系统导航栏 inset 又垫了一次；外层底栏已包含这块高度。
  周视图网格现在贴到底栏，后面的节次可以滑上来。

## [0.7.1] - 2026-09-10

### 新增

- **「上课中」实时状态**：今日课表与桌面小组件共用同一套判定（`TodaySnapshot.inProgress` / `remainingMinutes` 上移至 core:model）。
  - 今日页顶部新增上课中卡片：课程色描边、已上进度条、「还剩 X 分钟」倒计时（30 秒一拍随时间下走）。
  - 「下节课」小组件上课中显示「还剩X分」；「今日课程」小组件行标记「上课中」、时间列以「剩X分」
    替代下课时间（Glance 的 Text 无省略号能力，标签保持短于原「进行中 + 结束时间」口径以防裁切）。
  - 小组件刷新新增时钟轨（`WidgetAutoUpdater`）：App 进程存活期间，上课中每分钟推送一次驱动倒计时，
    课间/课前睡到下一个上课时刻或跨天零点再醒，不空转；课表/学期变化由数据轨推送并立即唤醒时钟轨
    重算唤醒点（避免睡过头错过新加的课），空闲期最长 1 小时醒一次兜底；进程死后的 24h 兜底不变。

## [0.7.0] - 2026-09-10

### 新增

- **中国科学技术大学教务适配器**（`jw.ustc.edu.cn` 综合教务），随 APK 内置。
  已登录后从首页即可提取：解析课表页拿到 studentId / 学期，再走
  `/for-std/course-table/get-data` 与 `/for-std/course-table/datum` 取选课与排课。
  作息时间按科大 13 节写入 `periodTimes`。fixture 为真实抓取（学号等敏感字段已剔除）。
  真机验证通过。
- **`allowHosts` 支持后缀通配**：`*.ustc.edu.cn` 匹配该域及其所有子域；通配后缀至少三段，
  拒绝 `*.edu.cn`。JS 沙箱、提取期原生闸门、OCR 下图共用同一套匹配。
  科大适配器声明 `*.ustc.edu.cn`，登录页打到 `analytics.ustc.edu.cn` 不再被沙箱误杀。
- **提取失败日志**：界面可复制；debug 包同时写入 logcat（tag `NullClass.Jw`）和
  `files/jw-last-error.log`（`adb shell run-as` 可读）。电脑版 / 手机版 UA 可切换。

### 修复

- **科大 `get-data` 返回 HTML**：提取请求 `Accept` 优先 `text/html` 时，教务按内容协商给网页，
  JSON 解析失败并误报「登录状态可能已失效」。JSON 接口改为明确要 `application/json`。

## [0.6.0] - 2026-09-09

### 新增

- **第一个真实教务适配器：大连工程学院**（原大连理工大学城市学院，`jw.dlutci.edu.cn`，
  金智教育 jwapp 平台），随 APK 内置。走四个同源接口取数（当前学生 → 当前学年学期 →
  校历 → 课表分页），周次直接用教务端的 `SKZC` 位串切块，单双周与跳周精确；
  学期名、开学日、总周数取自校历；附带学校作息时间（12 节）。
  fixture 为真实抓取数据（学号姓名已剔除、教师名脱敏）。金智平台经验：
  登录跳转目标不能是模块页（新会话直接落模块页会 403），适配器因此不指定
  `scheduleUrlHint`，WebView 从根地址起。

### 修复

- **「一键刷新」会把整份课表复制一份**：教务适配器与 WakeUp 迁移每次导入都重新生成
  记录 UUID，而导入合并按 ID 做 LWW，认不出两次导入的是同一批记录。新增导入对齐
  （`ImportAligner`）：同名学期复用本地 ID（当前学期标记沿用本地）；课程按名字+教师
  对齐并保留用户改过的备注与颜色；课块按内容对齐，学校改了时间/教室时旧课块作废，
  不留幽灵课；节次表的学期 ID 一并重映射。仅对重铸 UUID 的来源（`jw-*`、
  WakeUp 迁移）生效——备份、扫码沿用记录原 ID，仍走纯 LWW，导入旧备份不会作废
  本地较新的记录。

## [0.5.0] - 2026-09-08

### 新增

- **教务适配器平台化**：适配器从「写死在应用里的 Kotlin 接口」改为**一份可被第三方维护的包**
  （`manifest.json` + `extract.js` + 可选 `parse.js` + fixtures），同一份产物支持三种来源：
  - 内置：本仓库 `jw-adapters/`，构建时打进 APK
  - 社区：任何人 fork 的同构仓库，应用内粘 `index.json` 链接即可拉取安装
  - 用户添加：单个学校目录的 zip，SAF 选文件导入
- **适配器脚本契约升级**：支持异步（Promise / `__ncDone`）、多步请求；结果分块取回，
  修掉 `evaluateJavascript` 对超长字符串静默截断的隐患；脚本用 `new Function` 编译，语法错误可回报
- **网络白名单**：提取期间只放行同源与 `allowHosts` 声明的域，其余子资源/导航/JS 网络 API 全部拦截；
  提取结束后可在界面上一键恢复网页网络
- **一键刷新**：保留登录态，记住上次的学校与课表页地址，重进导入页直接加载并自动提取
- **图片课表 OCR**：随包内置 PP-OCRv6 tiny（det 1.8MB + rec 4.5MB，完全离线），
  课表是图片的学校也能导入；表格结构层做表头锚定 + 网格吸附 + 硬失败判定
- **第三方可调用的 OCR 桥**：`__ncOcr` / `__ncOcrGrid`，带调用次数与像素限额、能力发现
- 适配器管理界面：内置/用户分区、详情、脚本全文查看、删除、从链接添加、安装前风险确认
- `docs/jw-adapter-spec.md`：适配器规范（包格式、脚本契约、载荷、库、测试、安全红线）

### 变更

- `DefaultPeriodTimes` 从 `:core:data` 移到 `:core:model`（`:importer` 需要同一张节次表）
- 新模块 `:ocr`（ONNX Runtime + PP-OCRv6 tiny 模型），release 按 ABI 拆包
  （arm64-v8a / armeabi-v7a / x86_64 + universal）
- `importer` 的 CI 新增 Rhino 回归门：对 `jw-adapters/` 下每个适配器**实跑 `parse.js`** 比对 fixture

### 修复

- **http 教务系统打不开**：Android 9+ 默认全应用禁止明文流量，而规范明确允许 http 的 `loginUrl`
  （大量学校仍是 http-only），结果 WebView 直接报 `ERR_CLEARTEXT_NOT_PERMITTED`，
  从链接添加适配器库也一并失败。补 `network_security_config` 显式放开明文。
- **图片课表周次被教室号顶掉**：`parseWeeks` 的周次正则里「周」是可选的，于是「教1-101」
  被当成 1-10 周、「教3-201」被当成 3-20 周；真实格子常是「课名/教师/教室/周次」，
  教室行在周次行之前时就会静默写错周次。改为必须出现「周」才认（与规范 §4 一致），
  并补上「计算中心A」这类教室的识别。
- **`__ncOcr` 回传的是字符串不是对象**：桥把宿主传来的 JSON 字符串直接 resolve，
  按规范写的适配器拿到 `undefined`。改为解析成对象再 resolve。
- **OCR 桥对非默认端口从不注入**：`addWebMessageListener` 的 origin 规则丢了端口
  （规则 `http://host` 匹配不上 origin `http://host:8080`），教务系统跑在 8080/8081 时
  适配器完全拿不到 `__ncOcr`。origin 规则改为带端口，并按各 URL 自己的 scheme 生成。
- **`__ncCapabilities.ocr` 会撒谎**：能力位只看 OCR 引擎是否可用，不看桥是否真的注入。
  改为同时要求页面上存在桥对象。
- **一键刷新时适配器拿不到 OCR**：OCR 能力探测（首次要加载 ONNX 模型）在后台进行，
  而自动提取在 `onPageFinished` 就开跑 —— 运行器按「没有 OCR」构建脚本，
  适配器看到的 `__ncCapabilities.ocr` 是 false，直接放弃。提取前改为等能力探测出结果，
  并等桥对象真正注入页面（桥也改为在 `loadUrl` 前注册）。
- **从库更新已安装适配器不生效**：`JwAdapter.equals` 只比 key + 来源，同 key 重装后
  `MutableStateFlow` 认为状态没变、不发新值，界面继续用旧脚本直到杀进程。
  `equals`/`hashCode` 改为带上 manifest 与脚本内容。

### 移除

- `JwAdapter` Kotlin 接口与 `JwAdapterRegistry`（能力由适配器包与 `JwAdapterRepository` 取代）

## [0.4.6] - 2026-09-08

### 修复

- **课程时段冲突拦截**：同一学期内新建/编辑课程时检测与其他课程的时段重叠，
  冲突直接拦截、不落库（此前可保存出时段重叠的课程）

### 变更

- Compose `hiltViewModel` 导入迁移至 `androidx.hilt.lifecycle.viewmodel.compose`

## [0.4.5] - 2026-09-07

### 新增

- **底部三 Tab**：主界面改为「今日 / 课表 / 我的」三个顶层页
  - 今日：按上午/下午/晚上分组的一天课程，进行中高亮、已结束置灰，点卡片看详情
  - 课表：原周视图（start destination），顶栏「更多」菜单职责移交「我的」
  - 我的：学期卡片 + 导入/导出 + 应用设置入口
  - 详情页推入时隐藏底栏；Tab 间切换保留各自滚动/周次状态
- 今日列表与桌面小组件共用同一套「今天有哪些课」判定（`TodaySnapshot` 上移至 `core:model`）

## [0.4.4] - 2026-09-07

### 修复

- Navigation 恢复 2.10.0 —— v0.4.1 的降级属误诊，根因已在 v0.4.2 单独修复

## [0.4.3] - 2026-09-07

### 新增

- **桌面小组件一键添加**：设置页按钮经 `requestPinAppWidget` 直达系统确认层

## [0.4.2] - 2026-09-07

### 修复

- **保存学期后卡死纯色页**：连点导致 `popBackStack` 弹空返回栈（v0.4.1 降级的真正根因）

### 变更

- 周视图新增「当前时间」线，节次列布局调整，弹窗文案打磨
- 小组件尺寸与进行中文案调整；时间线可关闭且刷新更细

## [0.4.1] - 2026-09-07

### 修复

- 侧滑（预测返回手势）后保存学期白屏卡死
  （本版曾误判为 Navigation 2.10.0 缺陷并降级至 2.9.8，v0.4.4 已撤销）
- 预测返回手势跟随动画时的边缘白边 —— NavHost 底下垫不透明背景

### 变更

- 周视图交互与视觉打磨：显示设置弹窗、选周顶栏化、课块样式重调

## [0.4.0] - 2026-09-06

首个公开发布版。

### 新增

- **教务系统导入框架**：应用内 WebView 手工登录（空课不碰凭证）+ JS 注入提取课表，
  逐校适配器架构，社区可贡献（issue 模板 + README 章节就绪）；示例适配器演示全链路，
  提取结果复用导入预览/合并管线。真实学校适配器随请求逐步接入。

### 修复

- 两轮审查确认的 8 个缺陷：widget EntryPoint 聚合缺失、导入删除预警、WebView 泄漏、
  外部打开去重、扩展名兜底、WebDAV 编解码契约、WakeUp 告警、导入/同步互斥

### 变更

- 签名构建迁移到自托管 runner：签名材料留在服务器本地，GitHub 上零签名秘密
- 移除阶段性过程文档 `docs/impl`，并清理代码注释里指向它的悬空引用

## [0.3.0] - 2026-09-06

### 新增

- **课表导出**：全库导出为 `.nullclass` 文件（SAF 保存 / 系统分享），与 WebDAV 同步快照同构
- **课表导入**：应用内选择文件或系统「用其他应用打开」，预览学期/课程统计后合并导入
  （LWW 合并语义，同记录修改时间新者胜，不覆盖更新的本地数据）
- **二维码分享**：课表压缩生成二维码供扫码导入；超大课表自动提示改用文件分享
- **WakeUp 课表迁移**：导入 `.wakeup_schedule` 备份，连堂、单双周、节次时间表、
  颜色（最近色映射）全部保留；逐行内容识别，不依赖行号，兼容各版本 WakeUp 导出
- 共享格式统一：`.nullclass` 文件与 WebDAV snapshot.json 为同一 formatVersion 2 格式
  （导入 = 一次单向同步，删除传播/墓碑免费获得）

## [0.2.0] - 2026-09-06

### 新增

- **桌面小组件接真实数据**：
  - 「空课 · 今日课程」：今日课程列表，进行中/下一节高亮，跨尺寸自适应
  - 「空课 · 下节课」：紧凑 1×1 款
  - 课表变化自动刷新（数据推送）+ 每日维护 Worker 兜底（跨天）
- **课前提醒**：每节课开始前 5/15/30 分钟系统通知（可关闭），重排幂等，
  未授权通知权限不打扰；设置页可重新授权
- **自动同步**：WebDAV 定时同步（每 6 小时 / 每天），网络受限自动补跑

## [0.1.0] - 2026-09-05

### 新增

- 周视图课表：滑动切周、今天高亮、单双周过滤、连堂大课
- 课程/学期编辑：时间安排、教师、教室、12 色课表色板、自定义节次时间表
- WebDAV 手动同步：整库快照 + 逐记录 LWW 合并 + 删除传播
- 数据层：Room schema v2（UUID 主键 + 审计三列 + 软删除墓碑）
